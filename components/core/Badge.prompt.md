Small uppercase tracked eyebrow label, used above section headings ("The gap", "Solution", "Benefits", "FAQs").

```jsx
<Badge>The gap</Badge>
<Badge tone="onDark">Features</Badge>
```

Use `tone="onDark"` when the badge sits on the navy/dark background instead of the paper background.
