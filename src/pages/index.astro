---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import ProblemSolution from '../components/ProblemSolution.astro';
import BenefitsSection from '../components/BenefitsSection.astro';
import TestimonialsSection from '../components/TestimonialsSection.astro';
import FounderFeatures from '../components/FounderFeatures.astro';
import FAQCTA from '../components/FAQCTA.astro';
import { contact } from '../data/contact';
import { cities } from '../data/cities';

const structuredData = {
	'@context': 'https://schema.org',
	'@type': 'ProfessionalService',
	name: 'SumoLab',
	description: 'SumoLab provides fractional CMO leadership for established businesses generating $5M+ in annual revenue.',
	url: 'https://sumolab.co',
	telephone: contact.phoneHref ?? undefined,
	address: contact.location
		? {
				'@type': 'PostalAddress',
				addressLocality: contact.location.split(',')[0]?.trim(),
				addressRegion: 'AZ',
				addressCountry: 'US',
			}
		: undefined,
	areaServed: cities.map((city) => ({ '@type': 'City', name: `${city.name}, ${city.state}` })),
};
---
<Layout
	title="SumoLab | Proven Business Judgment, Real AI Results"
	description="AI won't fix your business — understanding it will. I bring both: decades as an operator and marketer, plus AI applied where it counts. No hype, just return."
	structuredData={structuredData}
	transparentHeader
>
	<Hero />
	<div class="content-wrap">
		<ProblemSolution />
		<BenefitsSection />
		<TestimonialsSection />
		<FounderFeatures />
		<FAQCTA />
	</div>
</Layout>

<style>
	.content-wrap {
		max-width: 1180px;
		margin: 0 auto;
		padding: 64px 40px;
	}
</style>
